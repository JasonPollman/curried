/**
 * A function that returns `undefined`.
 * A "no operation".
 *
 * @returns {undefined}
 * @category utility
 * @publishdoc
 * @since v0.0.0
 * @export
 * @example
 *
 * import { noop } from '@foldr/all';
 *
 * noop(); // => undefined
 */
export default function noop() {}
