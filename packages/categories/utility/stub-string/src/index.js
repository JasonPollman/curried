/**
 * A function that always returns an empty string.
 *
 * @name stubString
 * @returns {Object} A new string.
 *
 * @arity 0
 * @category utility
 * @publishdoc
 * @since v0.0.0
 * @export
 * @example
 *
 * import { stubString } from '@foldr/all';
 *
 * stubString(); // => ''
 */
export default function stubString() {
  return '';
}
