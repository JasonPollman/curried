/**
 * Exports the `f` function.
 * @since 10/29/18
 * @file
 */

/**
 * A function that always returns `false`.
 * @returns {boolean} The literal boolean `false`.
 * @category function
 * @publishdoc
 * @since v0.0.0
 * @export
 * @example
 * f(); // => false
 */
export default function f() {
  return false;
}
