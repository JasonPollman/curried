/**
 * Exports the `stubArray` function.
 * @since 11/18/18
 * @file
 */

/**
 * A function that always returns a new `Array` instance.
 *
 * @name stubArray
 * @returns {Object} A new, empty Array.
 *
 * @category utility
 * @publishdoc
 * @since v0.0.0
 * @export
 * @example
 *
 * stubArray(); // => []
 */
export default function stubArray() {
  return [];
}
