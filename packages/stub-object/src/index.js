/**
 * Exports the `stubObject` function.
 * @since 11/18/18
 * @file
 */

/**
 * A function that always returns a new `Object` instance.
 *
 * @name stubObject
 * @returns {Object} A new, empty object.
 *
 * @category utility
 * @publishdoc
 * @since v0.0.0
 * @export
 * @example
 *
 * stubObject(); // => {}
 */
export default function stubObject() {
  return {};
}
