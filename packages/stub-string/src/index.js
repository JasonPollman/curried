/**
 * Exports the `stubString` function.
 * @since 11/18/18
 * @file
 */

/**
 * A function that always returns an empty string.
 *
 * @name stubString
 * @returns {Object} A new string.
 *
 * @category utility
 * @publishdoc
 * @since v0.0.0
 * @export
 * @example
 *
 * stubString(); // => ''
 */
export default function stubString() {
  return '';
}
